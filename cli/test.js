const { gtokenize, gstokenize } = require("../tokenizer");
const tokenToString = require("./tokenToString");

console.log(gtokenize("Several underground organisations in BEN STEED CULT have recently been spreading discontent throughout the populace about not being able to vote."));
